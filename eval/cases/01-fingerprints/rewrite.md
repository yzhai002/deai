The caching layer stores results in memory and checks it before each query, so the user gets the cached copy without a fresh lookup.

A blind study found a 16 percent lift. More detail is available at https://example.com/report.
